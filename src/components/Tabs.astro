---
// Настройте эти значения под себя
const TELEGRAM_USERNAME = "Ja_6b";
const PREDEFINED_MESSAGE = "Здравствуйте! Хочу связаться с вами по поводу";

// Кодируем сообщение для URL
const encodedMessage = encodeURIComponent(PREDEFINED_MESSAGE);
const telegramLink = `https://t.me/${TELEGRAM_USERNAME}?text=${encodedMessage}`;



// Данные курсов — теперь они обрабатываются на этапе сборки Astro
const arrDataCourses = [    
  {
    filtreType: 'ege',
    type: 'ЕГЭ',
    item: 'Информатика',
    time: '11 класс',
    fullName: 'ЕГЭ по информатике',
    description: 'Подготовка к экзамену ЕГЭ по информатике с нуля до 90+ баллов',
    price: 12900
  },
  {
    filtreType: 'oge',
    type: 'ОГЭ',
    item: 'Информатика',
    time: '9 класс',
    fullName: 'ОГЭ по информатике',
    description: 'Эффективная подготовка к ОГЭ по информатике с разбором всех типовых задач',
    price: 7900
  },
  {
    filtreType: 'ege',
    type: 'ЕГЭ',
    item: 'Математика',
    time: '11 класс',
    fullName: 'ЕГЭ по математике (профиль)',
    description: 'Полный курс подготовки к профильному ЕГЭ по математике с нуля до 80+ баллов',
    price: 12900
  },
  {
    filtreType: 'oge',
    type: 'ОГЭ',
    item: 'Математика',
    time: '9 класс',
    fullName: 'ОГЭ по математике',
    description: 'Эффективная подготовка к ОГЭ по математике с разбором всех типовых задач',
    price: 7900
  },
  {
    filtreType: 'ege',
    type: 'ЕГЭ',
    item: 'Математика',
    time: '11 класс',
    fullName: 'ЕГЭ по математике (база)',
    description: 'Полный курс подготовки к профильному ЕГЭ по математике с нуля до 80+ баллов',
    price: 7900
  },
  {
    filtreType: 'prog',
    type: 'Программирование',
    item: 'Менторство',
    time: 'Для начинающих',
    fullName: 'Python для начинающих',
    description: 'Основы программирования на Python с нуля. Создай свои первые проекты!',
    price: 8900
  },
  {
    filtreType: 'prog',
    type: 'Программирование',
    item: 'Менторство',
    time: 'Для знатоков',
    fullName: 'Python для анализа данных',
    description: 'Освой Pandas, NumPy, Matplotlib и научись работать с реальными данными',
    price: 14900
  },
];

// Добавляем encodedMessage к каждому элементу
const arrDataCoursesWithMessages = arrDataCourses.map(course => ({
  ...course,
  telegramLink: `https://t.me/${TELEGRAM_USERNAME}?text=${encodeURIComponent(`${PREDEFINED_MESSAGE}
курса - '${course.fullName}'
описание - '${course.description}'
`)}`
  // encodedMessage: encodeURIComponent(`${PREDEFINED_MESSAGE}`) // ${course.fullName} ${course.description}
}));
---

<div id="main-content" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <!-- Секция курсов -->
  <section id="courses">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        Наши направления
      </h2>
      <p class="mt-4 max-w-2xl text-xl text-gray-500 mx-auto">
        Выберите направление подготовки и начните обучение уже сегодня
      </p>
    </div>

    <!-- Табы для фильтрации курсов -->
    <div class="w-full border-b border-gray-200 mb-8">
      <div class="flex flex-wrap justify-center max-w-5xl mx-auto">
        <button class="course-tab py-4 px-6 text-center border-b-2 font-medium text-sm text-gray-500 hover:text-gray-700 whitespace-nowrap tab-active" onclick="filterCourses('all', event)">
          Все курсы
        </button>
        <button class="course-tab py-4 px-6 text-center border-b-2 font-medium text-sm text-gray-500 hover:text-gray-700 whitespace-nowrap" onclick="filterCourses('ege', event)">
          ЕГЭ
        </button>
        <button class="course-tab py-4 px-6 text-center border-b-2 font-medium text-sm text-gray-500 hover:text-gray-700 whitespace-nowrap" onclick="filterCourses('oge', event)">
          ОГЭ
        </button>
        <button class="course-tab py-4 px-6 text-center border-b-2 font-medium text-sm text-gray-500 hover:text-gray-700 whitespace-nowrap" onclick="filterCourses('prog', event)">
          Программирование
        </button>
      </div>
    </div>

    <!-- Карточки курсов -->
    <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3" id="courses-container">
      {arrDataCoursesWithMessages.map((item) => (
        <div class={`course-card bg-white rounded-lg shadow-md overflow-hidden transition duration-300 ease-in-out ${item.filtreType}`}>
          <div class="relative">
            {/* <!-- Можно добавить изображение позже --> */}
            <div class="absolute top-4 right-4 bg-purple-600 text-white text-md font-bold px-2 py-1 rounded-full">
              -100%
            </div>
          </div>
          <div class="p-6">
            <div class="flex items-center">
              <span class="text-purple-600 text-sm font-semibold">{item.item}</span>
              <span class="mx-2 text-gray-300">|</span>
              <span class="text-gray-500 text-sm">{item.time}</span>
            </div>
            <h3 class="mt-2 text-lg font-bold text-gray-900">{item.fullName}</h3>
            <p class="mt-3 text-gray-600 text-sm">{item.description}</p>
            <div class="mt-4 flex items-center justify-between">
              <div>
                <span class="text-gray-900 font-bold text-lg line-through">{item.price}₽</span>
                <span class="text-purple-600 text-sm ml-2">Занятие бесплатно</span>
              </div>
              <a 
                class="bg-purple-600 hover:bg-purple-700 text-white px-4 py-2 rounded-md text-sm font-medium transition duration-150 ease-in-out" 
                href={item.telegramLink}
              >
                Запись
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>

<!-- Скрипты фильтрации и модалки остаются на клиенте -->
<script defer>
  function filterCourses(type, event) {
    // Убираем активный класс у всех табов
    document.querySelectorAll('.course-tab').forEach(tab => {
      tab.classList.remove('tab-active');
      tab.classList.remove('border-purple-600');
      tab.classList.add('border-transparent');
    });

    // Добавляем активный класс текущему табу
    event.target.classList.add('tab-active', 'border-purple-600');
    event.target.classList.remove('border-transparent');

    // Показываем/скрываем карточки
    document.querySelectorAll('.course-card').forEach(card => {
      if (type === 'all' || card.classList.contains(type)) {
        card.style.display = 'block';
      } else {
        card.style.display = 'none';
      }
    });
  }

  function showCourseModal(courseId) {
    console.log('Opening modal for:', courseId);
    // Здесь можно реализовать открытие модального окна
    alert('Функция открытия модального окна для курса: ' + courseId);
  }
</script>